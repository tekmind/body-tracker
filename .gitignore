node_modules
dist
.DS_Store
.env
# Local Claude state, except the preferences copy — the container is
# disposable and ~/.claude/CLAUDE.md isn't in git anywhere else.
.claude/*
!.claude/user-preferences.md
.obsidian

# Local env overrides — never commit these.
.env.local
.env.*.local

# Playwright screenshots written by the specs.
tests/screenshots/
